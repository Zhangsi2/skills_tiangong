interface:
  display_name: "Lifecycle Inventory Review"
  short_description: "Review local or frozen remote process/lifecyclemodel LCI outputs"
  default_prompt: "Use $lifecycleinventory-review to review local process_from_flow outputs, frozen remote process snapshots via the supported wrapper, or lifecyclemodel build artifacts through the TianGong CLI with reproducible inputs and outputs."
